import { cva } from 'class-variance-authority'

export const layout = cva('flex flex-col items-center gap-10')
export const heading = cva('display-small text-gray-900')
export const form = cva('flex flex-col gap-10')
